{
  "scripts": {
    "start": "parcel watch src/manifest.json --log-level verbose --host localhost --config @parcel/config-webextension",
    "build": "parcel build src/manifest.json --log-level verbose --config @parcel/config-webextension",
    "test": "node --import tsx --test tests/*test.ts"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.12.0",
    "@types/chrome": "^0.0.267",
    "parcel": "^2.12.0",
    "tsx": "^4.7.3",
    "typescript": "^5.4.5"
  }
}
